# Overnight report - WoW Addon Manager (2026-09-02)

Started 00:28 Central. Cutoff for starting new rounds: 07:00. Each round = audit -> fix -> expand -> review -> test (CLI, server, UI) -> deploy to `_retail_\AddonSync` if all tests pass.

## Round 0 - initial build (00:20 - 02:18) - DEPLOYED 02:18
- Scope: CLI extensions (-Only/-FileId/-Ignore/-Unpin/-Files/-Scan/-Json/-Launcher, pin/ignore/author, settings.json), local server (addon-server.ps1, 1715 lines), CurseForge-style UI (ui/, ~2170 lines JS).
- 23 agents. Review found 9 defects (6 CLI, 2 server, 1 UI) before testing. Tests: CLI 12/12 (2 rounds), server 12/12 (3 rounds - job lifecycle and scan/delete guard fixes), UI 8/8 first try, zero console errors.
- Deployed with backup `_retail_\AddonSync-backup-20260902-0218.zip`; live verification: 37 addons, server + UI round-trip OK. Desktop shortcut "WoW Addon Manager" created. Game launcher now passes -Launcher (honours the auto-update setting).
- Known low-severity notes carried into round 1: scan empty-state copy identical before/after scanning; README.txt still describes the CLI-only setup.
- Live verification at 02:19 (browser pane, 1320x900): renders cleanly, zero console errors. Found by me: (1) BUG - reloading the page fires the pagehide shutdown beacon, the server exits and the reload comes back unstyled; fix = delayed, cancellable shutdown (queued for round 2 as a mandatory fix); (2) all 37 rows show "Unknown author" because those records predate the author field - needs a metadata backfill during checks (round 2); (3) my own click on Check for updates missed after a viewport resize, so the live check path was verified via the API instead.

### Live findings after round 0 (02:20 - 02:30)
- CRITICAL, FIXED LIVE + IN BUILD: every job failed on the live install because the server passed the CLI path to the child process unquoted; under `C:\Program Files (x86)\...` the child died instantly ("Processing -File 'C:\Program' failed"). The build's tests never saw it because the scratch path has no spaces. Fix: quote the -File and -AddonsPath arguments (addon-server.ps1, New-CliProcessArgs). Verified live: check job 37/37 (36 up-to-date, MiniAuras update found) with the server responsive throughout; then a sync job updated MiniAuras 5.33.3 -> 5.33.4 in 3 s.
- CRITICAL, ROOT-CAUSED AND FIXED LIVE + IN BUILD (02:35): when a job's child process failed, the server hung forever (even /api/ping). Traced with injected logging to ConvertTo-Json inside Send-Json: the job's `error` text came from `Get-Content -Raw`, which decorates the string with PSPath/PSDrive/PSProvider note properties; ConvertTo-Json -Depth 12 then walks the provider/runspace object graph and never returns (confirmed in isolation: the same string via Get-Content hangs, via [IO.File]::ReadAllText serializes instantly). Fix: read child stdout/stderr with ReadAllText in Update-JobStatus and Invoke-Cli. Lesson for all PowerShell in this project: never put a Get-Content string into ConvertTo-Json. Round workflow now includes mandatory failure-path and spaced-path regressions in the server test stage.
- Round 2 mandatory fixes queued: delayed/cancellable shutdown (reload bug), author backfill for pre-existing records; plus verify no Get-Content-derived string reaches JSON anywhere (CLI -Scan/-Files included).

## Rounds

### Round 1 (02:19 - 04:27) - audit + E2 automatic update checks + E7 UI polish
- 25 agents. Audit: 17 findings (UX auditor independently reproduced the reload-kills-server bug and found the job panel covering new content). Fixes: pagehide shutdown beacon REMOVED (server relies on idle exit; reload is now safe), job panel auto-collapses on view switch, Versions tab gained a Game Versions column, row-actions column made sticky at 1000 px, five more unquoted spaced paths fixed in /api/open (notepad/explorer launches), -Files JSON schema deviation fixed, CLI multi-add usage documented correctly. Rejected as false positives with sound reasoning: StreamReader "leak", two missing-catch findings.
- E2: check results persisted to state.json (survive restarts), auto-check on UI load when stale (>10 min) and every 30 min, sidebar "n updates" badge. E7: status filter chips with counts, column sorting persisted, keyboard shortcuts (/ r Esc), density + light/dark theme, backups folder shortcut, tooltips, focus outlines.
- Tests: CLI 12/12 (3 rounds), server 29/29, UI 23/23 via browser, zero console errors.
- DEPLOYED 04:27 (backup `_retail_\AddonSync-backup-20260902-0427.zip`); post-deploy verification passed (parse, 37 addons, server + UI round-trip). Note: the spaced-path / failure-path server regressions were added to the round workflow after round 1 had started, so round 2 is the first round that runs them.

### Round 2 (04:28 - 06:57) - E1 rollback, E3 dependencies, E5 update digest + mandatory fixes
- 23 agents. Audit: 22 findings incl. the 3 mandatory ones (author backfill, Get-Content->JSON audit, unreachable-server banner + backoff) and UX-auditor finds: sidebar scrolled off-screen with the page (html/body height bug), toasts covering the toolbar buttons, "Pinned - vv1.1.2" double prefix, Versions-tab Install button clipped at 640 px. All applied.
- E1: previous zip kept per addon (max 2), previousFileId/previousVersion on records, `-Rollback`, job kind rollback, "Roll back to <version>" in the row menu, Previous tag in Versions. E3: requiredDeps/optionalDeps parsed from .toc, live missingDeps, warning chip + dependency list in the drawer with Search CurseForge. E5: "What changed" per updated row in the results panel, "Last run" summary line with Details.
- Tests: CLI 20/20, server 29/29 (incl. the new spaced-path and failure-path regressions - both pass), UI 19/21 (2 non-blocking: last-run/job history not persisted across a server restart; tester exceeded its request budget by 1 because the auto-check fired, i.e. correct behaviour). Residual advisories for the follow-up fix pass: rollback leaves record.fileName stale; finished jobs keep tailing sync.log; lastRun/job history reset on restart.

- DEPLOYED 06:58 (backup `_retail_\AddonSync-backup-20260902-0658.zip`). Live sync afterwards: 35 up-to-date, 2 updated (Raider.IO daily DB, Leatrix Plus 12.1.03), authors backfilled 37/37, no missing dependencies. Browser check of the live app: My Addons with authors/chips/last-run line, automatic check ran collapsed, drawer tabs, Settings - zero console errors.

### Round 3 (07:00 - 07:12) - targeted fix pass for round 2's advisories
- Single agent, all three verified live in a scratch dir (3 CurseForge requests): rollback now swaps fileName (new previousFileName field, round-2 sidecar hack removed); finished jobs stop tailing sync.log (log frozen at completion); lastRun + last 20 job views persisted in state.json and restored on start (never as 'running', id sequence continues past restored ids).

- DEPLOYED 07:09 (final state of the night; backup `_retail_\AddonSync-backup-20260902-0709.zip`). Post-deploy verification: parse, 37 addons, server + UI round-trip.

## Where things stand at 07:15
- Live: `_retail_\AddonSync` = round 2 + fix pass. 37 addons tracked, all current as of 07:00 (Raider.IO and Leatrix Plus updated tonight; MiniAuras earlier). Both desktop shortcuts work: "WoW (auto-update addons)" and "WoW Addon Manager".
- Features now in the app: installed list with authors, update badges, status chips + filter chips + sorting + keyboard shortcuts, per-addon update / versions (install any version) / pin / ignore / rollback / uninstall, dependency warnings, automatic update checks (on open when stale, every 30 min) persisted across restarts, update digest ("What changed") and last-run line, light/dark theme + density, Settings (release channel, auto-update on launch, API key with test, scan/adopt/delete untracked folders, open logs/backups, force reinstall), Update & Play / Launch WoW. Browse + descriptions/changelogs/screenshots light up once a free CurseForge API key is pasted in Settings.
- Not done (backlog, ROADMAP.md): E4 export/import addon list, E10 diagnostics panel, E11 bulk actions; deferred E6/E8/E9.
- Bugs found and fixed tonight that the build tests could not see: unquoted spaced paths (live jobs all failed), Get-Content strings hanging ConvertTo-Json (server froze after any failed job), reload killing the server, sidebar scrolling away, toast covering toolbar, pinned "vv" prefix, clipped Install button, /api/open path quoting, -Files JSON schema, rollback fileName, log tail pollution, non-persisted job history.
- Token use: ~11 M subagent tokens across 4 workflows/agents (25 + 23 + 23 + 1 agents).

### Round 4 (started 07:35, requested by Eric) - E4 export/import, E10 diagnostics, E11 bulk actions + all deferred low-severity bugs
- Live check first: the game-launch command (`addon-sync.ps1 -Launcher -Quiet`) ran in 18 s, 36 up-to-date, 1 updated, no warnings.

### Round 4 result (07:35 - ~09:50, deployed 12:50, pushed to GitHub)
- 19 agents. Audit 19 findings (all applied incl. every deferred low-severity bug: stale drawer tabs, ignore-menu icon, stale key-test message, "Pinning" job label, Add-dialog URL handling, empty filtered-list state, natural name sort, narrow-width Updated info, dependents listed in the uninstall confirm; plus the UX auditor's find that the 5-second poll rebuilt the table under the mouse and swallowed clicks - now skipped when state is unchanged).
- E4 export/import (Settings > Backup & restore), E10 diagnostics (Settings > Diagnostics with copy report), E11 bulk actions (checkboxes, Update/Ignore/Uninstall selected). Multi-id CLI arguments are joined with commas (the bug found during the audit is covered).
- Tests: CLI 8/8, server 27/27, UI 26/26, first try. Deployed 12:50 (backup AddonSync-backup-20260902-1250.zip) and committed to github.com/krenz444/furphy-addon-manager.

### Round 5 (12:52 - 15:52, deployed + pushed 15:53) - E12 Wago Addons source (keyless), E13 compatibility column, cleanups
- 21 agents. Audit 27 findings applied (notable: CurseForge API key could leak into an error message from the key-test path - fixed; unbounded proxy cache - bounded; URL-encoding in /api/open; aria-selected on drawer tabs; About uptime freezing; console.curseforge.com links in the no-key panel; mock data no longer names WeakAuras; docs state Midnight/S2 context; /api/open gained serverlog).
- E12: records carry source/slug/wagoId/curseId; `-Add wago:<slug>`/Wago URLs; Wago releases/versions/pin/rollback/remove; server /api/wago/search|categories|resolve|addons/{slug}|releases|gallery with Inertia version cached in state.json; UI Browse source switch, Wago badges, "Also on" cross-source actions, Wago changelogs keyless. E13: compat per record (ok / stale-minor / stale / unknown) from .toc Interfaces + newest file game versions vs the client build from .build.info; Compatibility column + Stale filter; client build in diagnostics and Settings.
- Tests: CLI 10/10, server 25/25, UI 17/17 (2nd attempt). CAVEAT: the testers verified Wago offline only (fixtures/mocks) - live addons.wago.io verification done by hand after deploy (see below).
- Live Wago verification (16:00, isolated copy + scratch AddOns): /api/wago/search "bonus roll" -> 4 results; details (id nKW1zLNE, downloads, categories); releases with changelog + supported patches; add job installed BonusRollPreview from Wago (record source=wago, release WwOPbDl6); sync -> Up-to-date; remove -> clean; zero warnings.
- Found during live checks and fixed in a follow-up pass (deployed + pushed 16:10, commit 35d7f61): compatibility false positives on BigWigs Classic/Wrath/Cataclysm (multi-flavor packages; the base .toc is the Cataclysm-Classic one - now Mainline-first selection and Interface-Mainline support), Wago records' compat, wagoId/curseId backfill for existing records (24 known on Wago, 28 on CurseForge), Wago search thumbnails (unquoted src attribute). Live compat now: 34 ok, 3 stale-minor.
- Live UI check (16:20, browser): My Addons shows the client build, "3 addons need attention", Compatibility chips (Built for 12.1 / Older patch), Stale filter, CF source badges, bulk checkboxes; Browse > Wago Addons lists the popularity grid with thumbnails; a Wago addon's drawer shows downloads/likes/updated, Versions (10 releases with size, channel, retail patches, per-version changelog, Install) and a rendered Changelog with version selector - all keyless, zero console errors. BUG FOUND: typing a search term in the Wago tab never filtered, because the UI sends sort=popular and Wago silently drops the search term for any sort value other than "name" (and "updated" returns nothing). Fix pass launched; the round workflow now has a mandatory LIVE Wago regression so offline-only verification can no longer pass.
- Fix deployed + pushed 16:18 (commit c149768): server forwards sort only as `name`, otherwise omits it; Wago sort options reduced to Popularity/Name; SPEC updated. Live API: "bonus roll" -> 4 hits, bonusrollpreview first. EllesmereUI 9.1.6 and MiniAuras 5.34.0 installed via the app's own auto-check + Update all (37/37 current).

### Midnight Season 2 compatibility audit (08:40)
- Client 12.1.0.69587 (Interface 120100). 34/37 addons are on today's newest CurseForge file AND declare 120100 / are tagged 12.1.0 (AlterEgo 1.6.5 and MDT 6.2.11 pulled during the audit). WoW's `checkAddonVersion` is 0, so out-of-date addons still load.
- Stale (not built for 12.1): BetterAddonList (1.4.7, 2026-06-14, 12.0.7), MinimapButtonBag-Reborn (4.0.29, 2026-04-22, 12.0.5), X Practice (2026-07-07, 12.0.7). Research on function/successors running (results appended below).
- BigWigs Cataclysm/Dragonflight modules have old dates but are explicitly tagged 12.1.0 by the authors - fine.
- WeakAuras no longer exists in Midnight; the only mention left in the app is dev-only mock data (queued for cleanup, NEXT-FIXES.md). Compatibility signals are specced as ROADMAP E13 so this audit becomes a permanent column in the app.
- Research verdicts (each independently verified by a second agent trying to refute it; none refuted):
  - BetterAddonList: works-but-stale, KEEP. Non-combat addon-list UI (search, sets, protect, memory column) by nebularg (BigWigs maintainer); zero 12.1 breakage reports; nothing in EllesmereUI/EQoL replaces it.
  - MinimapButtonBag-Reborn: works-but-stale, REPLACE. Maintainer silent since Dec 2025 (open "Midnight support?" issue unanswered). Enhance QoL's "Minimap Button Sink" module (12.9.9, tagged 12.1.0) and EllesmereUI's minimap flyout already cover it - enable one in-game, then remove MBB.
  - X Practice: works-but-stale, KEEP (low confidence). Raid-mechanics rehearsal room using ModelScene; author still shipping (July), no 12.1 file yet, no breakage reports, no substitute exists.
  - Patch facts: 12.1.0 "Curse of Ula'tek" released 2026-08-11, Season 2 raid unlocked 2026-08-18.

## Needs your decision / action
1. Browse/search, descriptions, changelogs, screenshots and logos need a free CurseForge API key: console.curseforge.com -> sign in -> API Keys -> generate -> paste in Settings -> Test. Two minutes; everything else works without it.
2. The Wago app is still installed (closed, not uninstalled) - uninstall when you are happy; `WagoAppCompanion` was already removed.
3. Rollback keeps up to 2 previous zips per addon in `AddonSync\backups\` (~70 MB now, capped by the per-addon limit). Fine to leave; "Open backups folder" is in Settings > Maintenance.
4. Timeline Reminders and AuraUpdater cannot be automated (Patreon-only distribution by their author) - they stay removed; both are in `_retail_\AddOns-removed-2026-09-01.zip` if you want them back by hand.
5. If you want the remaining backlog (export/import, diagnostics, bulk actions) built, say so - the round workflow is ready to run again.

### 18:50 - MinimapButtonBag-Reborn removed (Eric's decision)
- Uninstalled via the CLI (folder MBB deleted, record dropped); 36 addons remain, all current. Enable Enhance QoL's Minimap Button Sink or EllesmereUI's minimap flyout in-game to collect minimap buttons.

### Round 7 (19:40 - 20:28, deployed + pushed 20:28, commit f460387) - Furphy Addon Manager branding, icon, Vaporwave theme, API-key field fix
- CurseForge key: Eric's first token was a UUID (author upload token, not a Core API key) and the second paste was swallowed by the masked field; the console key ($2a$10$...) is now saved and valid (test ok, 71 categories via proxy). Fixed the field UX (clears on focus, explicit Saved/No-change toasts, Test names which key it tested, Clear-key button) and the 401/403 logo-batch retry storm (sticky rejected flag + backoff + Browse panel).
- Icon: three vaporwave concepts rendered via headless Edge; chosen sunset + puzzle piece. make-icon.ps1 renders 16..512 PNGs and packs a 7-size PNG .ico; assets: ui/icon.svg, ui/icons/furphy-*.png, icon.ico, ui/manifest.json. Desktop shortcut renamed "Furphy Addon Manager" with the icon.
- Rename: title, sidebar brand, About, /api/ping name, vbs, README, SPEC/CHANGELOG headings. Folder/file names unchanged.
- Vaporwave theme: complete token override (23 tokens + theme-scoped extras), neon glows, gradient brand, CSS perspective grid, neon chips, cyan focus rings; five hard-coded colours routed through tokens so all themes stay consistent; contrast >= 4.79:1 worst case. Browser test 6/6, zero console errors.
- 20:30 follow-up (commit 3b5f9d3): sidebar brand on one line with a 26 px icon. Live verified: logos load with the valid key, Vaporwave renders, zero console errors.
- 21:43 (commit 76849d7): Vaporwave is the default theme (Eric: "looks sick"); Dark/Light remain selectable. Keyless-mode research workflow launched (WoWInterface API, WowUp hub, catalogues, Wago-as-metadata, CurseForge edges) -> E16 spec.

### Keyless-mode research (21:45 - 22:05) - 10 agents, 4 probes verified, none refuted
- addon-radar.com: keyless hourly CurseForge mirror by project id (logo, description, screenshots, categories, game versions, dates, downloads, fuzzy search). instawow-data catalogue: CF id -> name/slug resolution 37/37. Wago-as-metadata: 30/37 via wagoId or confident name match. Dead ends: WowUp hub (own catalog), strongbox CF catalogue (frozen 2022), CF RSS/legacy/edge endpoints. Design appended as ROADMAP E16 (fallback order Wago -> addon-radar -> CF files-only; Browse = Wago + addon-radar search). Risk: addon-radar devalue decoding in PS 5.1.

### Round 8 (started 22:40) - E16 keyless CurseForge search/metadata (addon-radar + instawow catalogue + Wago match), E17 curseforge:// install links + "Search on CurseForge", auto-check panel fix
- Eric's goal restated: anyone can install and use Furphy without an API key. CORS probe from the app origin in headless Edge: CurseForge search -> "Failed to fetch" (no Access-Control-Allow-Origin); direct in-page search ruled out. E18 (installer, adoption, package, external README) queued for round 9.
- Round 8 result (deployed + pushed 2026-09-03 11:06, commit 5c60d6f): E16 keyless CurseForge search/metadata built and tested (CLI 13/13, server 17/17, UI 12/12). E17 (curseforge:// handler) was BLOCKED by the build safety classifier - not built; to be decided with Eric (opt-in registration). Auditors again flagged Vaporwave-default and no-shutdown-beacon; fixer correctly rejected both; SPEC now records both as decisions. Markdown list rendering in Wago descriptions fixed; auto-check panel collapsed by default.

### Round 9 (started 2026-09-03 11:08) - E18 installer / no-key-required for other users, Search-on-CurseForge side window, key labelled Optional
- 2026-09-03 11:25: FOUND + FIXED a launcher-breaking bug: VBScript CInt overflows above 32767, so parsing port 47831 crashed Addon Manager.vbs (desktop shortcut) - now CLng, fixed live. E17 written by hand (register-protocol.ps1 opt-in + curseforge-handler.vbs), live-tested in isolation (install at fileId -> pinned; second click -> already managed) and registered on this machine at Eric request. E19 spec written (WebView2 host, embedded CurseForge tab, ad filter OFF by default). Repo push of these files deferred to the next deploy step.
- Round 9 result (deployed + pushed 2026-09-03 12:55, commit 017e810): E18 built - install.ps1 / Install Furphy.cmd (WoW detection, copy, launcher rewrite, shortcuts, optional protocol registration, adoption from .toc ids, idempotent, -Uninstall), package.ps1 + VERSION 1.0.0 (dist zip 444 KB), external README.md ("No API key needed"), first-run adopt dialog; Search-on-CurseForge side window; key labelled Optional; addon-radar devalue decoder bug fixed (real: keyless enrich tier was returning null). Tests CLI 10/10, server 20/20, UI 13/13. Round 10 (E19 native host + ad filter option + E17 Settings wiring) launched 12:57.

### Round 10 (12:57 - 14:02, deployed + pushed 14:02, commit e6f3787) - E19 native Furphy window
- host\FurphyHost.cs (C# 5, WinForms + WebView2, 1345 lines) compiled on the machine by build-host.ps1; tabs Furphy | CurseForge (toolbar back/forward/home/search); curseforge:// and /install/<fileId> links intercepted in-app (no registry needed) -> add / install / add-by-slug jobs; ad filter option (settings.adFilter, OFF by default, 153-host list, hard allow-list for curseforge.com/forgecdn/overwolf/cloudflare/localhost, note text in Settings > Browsing); hostWindow persisted; exit 3 when the WebView2 runtime is missing (Edge --app fallback in Addon Manager.vbs). Server: adFilter/hostWindow settings, /api/ping host kind, add-by-slug, /api/protocol/status|register|unregister; UI: Browsing card + curseforge:// toggle with Undo (E17 wiring). Installer copies host\ and rebuilds the exe. Review caught 5 unquoted-path bugs in install.ps1 before shipping. Self-tests 8/8. Live: native window launched against the live server (title OK, host=webview2). Release zip rebuilt with the host.
- 14:06 (commit c28fff2): package.ps1 now ships host\ (12 files); dist\FurphyAddonManager-1.0.0.zip = 1014 KB, 37 entries, no state files. Waiting for Eric to approve the GitHub Release.
- 14:10: Eric opened the native window: WinForms chrome (tab strip/toolbar) drawn in light system colours and the window blurry (process not DPI-aware on a high-DPI display). Mitigation: host exe disabled live (shortcut falls back to Edge), fix agent running (SetProcessDpiAwarenessContext PerMonitorV2, dark custom tab bar/toolbar, DefaultBackgroundColor, selftest records dpi + chrome pixels). Update all applied 8 updates (BigWigs 424.5, MDT 6.2.12, EQoL 12.9.11, NSRT 12.1.19, Raider.IO, MiniAuras 5.35.0, RareScanner 12.1.0.8, BigWigs Classic 12.1.2).
- 14:12: Eric: new default theme "Lofi Night" (pixel night cityscape + lofi cats) replacing Vaporwave as default - round 11 workflow running (CSS/SVG only, offline).

### Round 11 (14:12 - 15:05, deployed + pushed 15:05) - Lofi Night default theme + native host DPI/chrome fix
- Native host: cause confirmed - primary 5120x2160 monitor at 125% scaling, secondary at 100%; the host process was not DPI-aware so Windows bitmap-stretched it (blur) and WinForms drew its tab strip/toolbar in light system colours. Fix: SetProcessDpiAwarenessContext(PerMonitorV2) before any window, owner-drawn dark tab bar (#12081f) and toolbar (#1a0b2e), DefaultBackgroundColor; selftest records dpiAware + chrome pixels (8/8 incl. install intercept + job post 202). Host binaries pushed live first (14:40) and the exe re-enabled; full deploy follows here.
- Lofi Night (value "lofi", E15b): complete token block, pixel-art SVG cityscape (stars, moon, ~60 lit windows), two lofi cats (one tail sways, one asleep) + brand cat, amber accent glow, reduced-motion gate. Default for fresh installs; Dark/Light/Vaporwave unchanged. Contrast audit found --text-faint at 3.6:1 on bg-3 -> lightened to #9795b0 (>= 4.56:1 everywhere). Browser verification 9/9 (default load, persistence, all four themes, no horizontal scroll at 1000px, zero console errors). Release zip rebuilt after this deploy; GitHub Release still awaiting Eric's go-ahead.

### Round 12 (15:20 - 17:45, deployed + pushed 17:45) - cf-window fix, page->host messaging, theme-matched title bar
- Eric hit "Couldn't open that: unknown what: cf-window" (Browse > Search on CurseForge.com): round 9 shipped only the UI half of the 'cf-window' /api/open target. Now: server opens a chromeless msedge --app side window (curseforge.com only, URL sanitised - reviewer caught quote/CRLF and then space injection into the Edge argv); in the native host the same box, "Open on CurseForge" and any curseforge.com link open in the embedded CurseForge tab via window.chrome.webview.postMessage {type:"open-curseforge"}.
- Eric: "skin the top status bar to be the same color": page posts {type:"theme", name, colors{bg0..accent}} on every theme apply; host recolours tab bar/toolbar/search box/webview backgrounds and sets DWMWA_USE_IMMERSIVE_DARK_MODE(20)/CAPTION_COLOR(35)/TEXT_COLOR(36)/BORDER_COLOR(34) (COLORREF 0x00BBGGRR); persisted as settings.hostTheme (validated server-side: name ^[a-z0-9-]{1,32}$ case-sensitive, <=12 #rrggbb colours) so the next launch paints correctly before the page loads. Host default palette is now Lofi Night.
- Reviews (4 rounds) caught: host trusting messages from the CurseForge tab's webview (now only the app page), argv injection in Open-CfSideWindow, unvalidated theme name, selftest persisting its test palette into the real settings.json (persist disabled under --selftest), a selftest message-ordering race, docs claiming the host half was unimplemented. Verify 12/12: marker themeBg0 #102030, tabBarPixel #102030, toolbarPixel #203040, captionHresult 0, darkModeHresult 0, openCurseforgeUrl bigwigs, cfTabActiveAfterMessage true, intercept + job post 202 intact; server 400s for missing/non-CF url; hostTheme round-trip + rejection; UI mock in and out of host mode; node --check + PSParser clean.
- 2026-09-04 05:20Z: Eric said 'publish 1.0.0' - GitHub Release v1.0.0 published (tag on d8f8a1b, asset FurphyAddonManager-1.0.0.zip 1049 KB, sha256 c3d9f2e3f5a97e27499f9f33ec292b814b8c76bf9932c97d9c5b3567c3dfe2fd): https://github.com/krenz444/furphy-addon-manager/releases/tag/v1.0.0

### Round 13/14 (2026-09-04 ~04:00 - 07:10, deployed + pushed 07:15) - UX pass: clean experience (v1.1.0)
- Eric: "full pass ... clean experience, open the app, update addons, progress bars, no extra textual fluff, easy to understand terms, unified settings, simple, all in one, easy search for new addons, clean control of installed addons and clarity of if everything is out of date, prune unneeded information".
- Design: 4 mapping readers (UI copy inventory, progress pipeline, search data flow, settings) -> 3 designers (minimal / task-flow / status-clarity) -> 3 judges (task-flow won 152/159) -> UX-SPEC.md (authoritative UI spec, now mirrored to repo docs/).
- Build (sequential CS1-CS6): CLI writes jobs\<id>.progress.json atomically (queued/checking/downloading/installing/done/failed + bytesDone/bytesTotal from a new HttpWebRequest streaming download inside the existing retry wrappers); server exposes job.progress and /api/state freshness (not_checked/checking/up_to_date/updates_available/check_failed) + lastCheckFailed/lastCheckError, /api/open 'logs'; UI: Components.Freshness (one headline, sidebar dot silent when healthy), 5-column table with one prioritised Status pill (Update/Retry act as buttons), JobPanel determinate bar + current item + raw log behind Details, one results list with plain failure reasons + per-row Retry, drawer name bug fixed, Browse = one box searching CF + Wago concurrently into one badged grid (no tabs/sort/categories; fallback links to CurseForge.com / paste a link), Settings = Essentials (Updates 2 toggles, Appearance, collapsed optional key) + one Advanced disclosure, copy sweep (Take over, no project id/toc/keyless/adopt/digest/stale), ?view= deep links. 7 review rounds fixed: Update & Play skipping the bar, byte progress not rendered, failed uninstall flipping freshness, Retry on failed rollback posting a sync, drawer 'Compatibility' heading, tense mismatch in panel titles, Light-theme chip contrast. Verify: 34/35 then the last contrast token fixed; live jobs proved phases + bytes (7,090,763 B) + freshness transitions; CLI -Status -Json keys unchanged; final high-only review clean. Screenshots in shots\.
- 2026-09-04 13:58Z: Eric said 'publish 1.1.0 and install it on my desktop' - GitHub Release v1.1.0 published as Latest (tag on 491d421, FurphyAddonManager-1.1.0.zip 1080 KB, sha256 6afcd0a31d7a56877b3cd7616555dadb5914df88e0b61ba848062642271c22cc): https://github.com/krenz444/furphy-addon-manager/releases/tag/v1.1.0 . The same package was then installed on Eric's PC via install.ps1 -SkipAdopt over the live folder (exit 0: files + ui, host rebuilt, launchers rewritten, both desktop shortcuts recreated, curseforge:// re-registered, settings/addons untouched); app relaunched, /api/ping 1.1.0, 33 addons.

### Round 15 (2026-09-04 08:30 - 10:30, deployed + pushed 10:40) - Get new addons: Wago search | CurseForge site, host tab strip removed (v1.2.0)
- Eric: "i dont like the weird tab thing on the left with furphy and curseforge - get rid of that. rename browse to get new addons. at the top ... switch between wago in app search, and curseforge, which browses the site contained in the area that the wago search was in, one unified experience".
- Host: deleted _navPanel/HostTab/ActivateTab and the WinForms CF toolbar; the SPA now fills the window and the CurseForge WebView2 is an overlay child of the same panel that THE PAGE positions. New two-way channel (SPEC E21): page -> host cf-show{rect,dpr,url?}/cf-rect/cf-hide/cf-nav{back,forward,reload,home,go} (curseforge.com allow-listed, accepted only from the Furphy webview); host -> page host-ready{version,capabilities:[cf-pane]}/cf-state{url,title,canGoBack,canGoForward,loading}/cf-job{jobId}. New --view/--tab args. Selftest marker drops tabBarPixel/toolbarPixel, gains hostReadySent/cfShowCount/cfHideCount/cfPaneVisible/cfPaneBounds/cfStateMessages/cfLastUrl + a PNG capture of the window; deep-link interception is now injected during selftest so intercepted/jobPostStatus still prove out.
- SPA: "Browse" -> "Get new addons" everywhere visible (?view=browse aliased); segmented [Wago | CurseForge] at the top (localStorage, default Wago); Wago segment = in-app search, Wago-only, unchanged card/install machinery, fallback "Not on Wago? Try CurseForge" switches segments (native) or opens the side window (Edge); CurseForge segment = HTML toolbar (back/forward/reload/home/search/title) + a placeholder the host paints over, rect posted via getBoundingClientRect + devicePixelRatio, rAF-throttled from a ResizeObserver + resize/scroll, cf-hide on any dialog/drawer/popover and on leaving the view; job panel renders inline in that segment; toasts move over the sidebar. Edge window gets a compact "browsing happens in the desktop window" panel.
- Review/verify: 2 review rounds (selftest capture stealing focus, docs lag, a CSS class collision with the Settings toggle) then verify 19/21 -> fixed (selftest never exercised interception; host.log missing cf-show lines) -> 17/17 clean; final high-only review clean. Captures: shots\cf-pane-printwindow.png, wago-pane-printwindow.png, selftest-marker2.png. Note: captures used PrintWindow rather than CopyFromScreen to avoid stealing focus from Eric's game.

### Round 16 (2026-09-04 12:00 - 16:20, deployed + pushed 16:30) - CurseForge focus view, ad filter on by default, API key removed (v1.3.0)
- Eric: "only render the actual search results content ... even block the sidebar that has the filters", then "get rid of cookie banner, and the other banner that appears on top, and anything else other than the content, turn on ad block by default, and get rid of anything on the website unrelated to the content we need", then "i want the curse api key cleared, i want that removed as a feature".
- E22 focus view: host injects a page-type-aware trimming script into the CF pane (AddScriptToExecuteOnDocumentCreated + ExecuteScriptAsync for live toggling both ways). Listing/search: promo strip, global nav, hero, Discover/Browse-All row, filter sidebar, active-filter chips and footer hidden, results column promoted to full width, content starts at the project-count/sort row. Project pages: keep title/author/description/screenshots/files/download, hide nav, footer, ad rails, upsells, related-projects shelf and comments. Any other URL: untouched. Consent/overlay hiding runs on every page kind (visual only - the injected JS contains no click/dispatchEvent/document.cookie; verified by grep) and restores scroll/pointer-events. Real banner identified from the app's own fresh WebView2 profile as #cookiebar/.cookiebar (renders ~4 s after navigation, so a single NavigationCompleted check misses it) plus known CMP vendor selectors and a fixed/sticky text heuristic scoped to body's first two levels. Fail-safe: if the results container is missing or the trim would empty the pane, nothing is applied and the marker records the reason.
- adFilter default flipped to ON everywhere it is defaulted (server, installer, deploy, Mock, docs) and the standing SPEC decision text rewritten so audits stop reverting it; new CF ad/CMP hosts appended to adfilter-hosts.txt.
- E23: the CurseForge API key is GONE as a feature - Settings section, save/test/clear handlers, POST /api/settings/test-key, Invoke-CfApi and every key-gated /api/cf/* route, cfApiKey in server+CLI settings, hasApiKey/apiKeyHint, the 401-rejection machinery, LogoCache and the keyed drawer path. Keyless enrichment is now the only metadata path; "add a key" nudges became plain "No description available." A migration drops a stored cfApiKey from settings.json on first read (atomic rewrite, value never logged). NOTE: the Add-addon dialog's paste-a-CurseForge-URL option went with /api/cf/resolve - numeric id and wago.io links still work, and the embedded CF site's Install button covers the rest.
- Review/verify: 4 review rounds (stale rAF disabling a healthy trim; mobile bottom-nav leaking and the count/sort row being lost at narrow widths; adFilter still defaulting OFF in the host's own settings read; a deleted icon sprite still referenced by the Ignore-updates row; project-page ad rail and related shelf) then verify 10/12 -> the real catch: at the app's actual pane width (760 px) CurseForge serves its MOBILE layout and the trim skipped itself as 'skipped-narrow-viewport', so the feature never activated where it mattered -> fixed -> 13/13 clean, final high-only review clean. Captures in shots\focus-*.png.
- Behaviour note for future rounds: a raw PUT /api/settings {cfFocus:false} does NOT apply live to an open window; only the SPA toggle's {type:"cf-focus"} message does (the host otherwise re-reads settings at the next navigation start).

### Rounds 16.5 + 17 + tray stage A (2026-09-04 15:00 - 17:00, deployed + pushed ~17:05) - v1.4.0
- 16.5: search/filter fields - the nested input no longer repaints/outlines inside the pill (global input:focus / :focus-visible rules scoped out; the pill shows focus via border + accent-tint ring), My Addons filter + Wago search grow to 480 px, CurseForge toolbar search to 520 px. Verified 6/6 in all four themes.
- 17: CurseForge pane fills to the window bottom (flex chain replaces a calc(100vh-232px) magic number; .main.is-cf-pane-active); Wago results are full-width CurseForge-style rows (.browse-row, keyboard-accessible); "Update addons before WoW starts" + two honest lines (only Furphy's shortcut / Update & Play run the updater - verified: no hook/task exists); the checkAddonVersion block and its server reader removed; the manual catalogue-refresh button/route removed (daily auto-refresh kept); Vaporwave default again (SPA side). Verified with real-window captures.
- Tray stage A (host only): FurphyHost.exe --tray (NotifyIcon, hover status, click opens/activates the app, menu Open/Check now/Start with Windows/Quit, 90 s first cycle then settings interval, WoW-running skip, server autostart with CreateNoWindow, 409 busy handling, balloon only on updated/failed, tray-state.json, Mutex FurphyAddonManager.Tray, Event FurphyAddonManager.TrayStop, HKCU Run "FurphyAddonManager"), --tray-selftest, --wow-fake. Verified 7/7 (skip proven against Eric's live game). Disabled by default; no UI yet (stage B).
- False alarm worth recording: a "cf-focus regression" after the tray refactor turned out to be a stale cfFocusEnabled='0' in the BUILD-ROOT exe's WebView2 profile from a 14:21 toggle test (the deployed exe has its own profile). Design flaw exposed: the pane's localStorage flag overrides settings.json instead of the reverse - fix queued for round 18. Lesson: wipe host\bin\FurphyHost.exe.WebView2 between test runs, and always test the small (mobile-layout) window.

### Round 18 (2026-09-04 17:05 - 18:50, deployed + pushed ~18:55) - tray stage B, cf-focus authority, 10 themes + swatch picker (v1.5.0)
- Tray stage B: settings backgroundUpdates/backgroundIntervalMinutes(30..1440 clamp)/runAtStartup; GET /api/tray/status (pid-verified), POST /api/tray/start (409 if running, 501 if exe missing) / stop (sets EventWaitHandle FurphyAddonManager.TrayStop), POST /api/startup/register|unregister + status (HKCU Run "FurphyAddonManager" = "<exe>" --tray, byte-identical to the host's own); install.ps1 -Uninstall removes the Run value, stops the tray, waits. Settings > Updates: "Update addons in the background" + interval (hour/2h/4h/8h/daily) + "Start with Windows" (turning it on also turns background updates on; off leaves them) + one status line from tray-state.json. Verified against the REAL server from the real Settings page: tray process starts/stops, status line updates, Run value written/removed, no double start.
- Host: SyncCfFocusToPage pushes settings.json's cfFocus into the pane on init and every NavigationCompleted (settings authoritative; localStorage is a cache) - proven with a stale '0' profile; default title-bar palette is Vaporwave.
- Themes: ten blocks verbatim from THEMES-SPEC.md (terminal-green, arctic-ice, art-deco-gold, alpine-dawn, matcha, desert-night, tokyo-rain, brushed-steel, aurora-sky, strawberry-cream) with chevron overrides, colocated swatch colours and light signature touches; 14-theme swatch-grid picker (ARIA radiogroup, arrows move focus, Enter/Space commit) fed by one THEMES array; ?theme=<slug> deep link. Live contrast script: all 14 themes pass 4.5:1. Verified 6/6 with headless captures of every theme.
- Pre-existing, not fixed here (for the adversarial bug round): non-numeric strings for int settings return 500 with a raw .NET message instead of 400; bool settings coerce truthy strings.

### Round 19 (2026-09-04 19:00 - 23:30, deployed + pushed ~23:40) - multi-version: Retail / Classic / Classic Era (v1.6.0)
- FLAVORS-SPEC.md implemented as CS-F1..F7: Get-InstalledFlavours (folder + Interface\AddOns primary, .build.info corroborates), dynamic Interface-range table for Classic's rolling progression (verified live: editing the fixture's wow_classic row flips Mists->Cataclysm/77522 with zero code change), -Flavor/-WowRoot, per-flavour flavours\<id>\{addons.json,state.json,backups}, copy-first idempotent migration with schemaVersion 2 and a documented rollback, ?flavour= on every addon-scoped endpoint (400 only when >1 flavour is installed and the param is missing), per-flavour jobs/freshness, install-flavour resolution for curseforge:// links (auto/refuse/ask + SPA chooser), the switcher (zero DOM at one flavour; Era tooltip "Includes Hardcore & Anniversary realms"), Update All, per-flavour launchers with the honest Battle.net note, tray update-all-flavours fan-out, WowDetector exact names for all clients, fixture under fixtures\wowroot.
- Reviews fixed: unresolvable Classic era silently mapping to Retail ids (now fails loudly), ?flavour= validation bypass when zero flavours detected (path-construction risk), awaiting_flavour job the SPA never surfaced, Get-PrimaryTocFile falling back to the Retail toc selector for an unknown era (both copies), and after verify #1 failed: the SPA never booted on a multi-flavour server (Store started with installedFlavours [] so it never sent ?flavour= and every /api/state call 400'd) - fixed, verify #2 7/7.
- Migration rehearsal on a COPY of Eric's live state: 34 records, 23 backup files, md5-identical in flavours\retail and the backup folder; rollback restored byte-for-byte; live files untouched (hashes checked before/after).
- FINAL REVIEW HIGH: install.ps1 -Uninstall deleted the REAL Desktop shortcuts unconditionally (no -WowPath/-NoShortcuts scoping) - it did so on Eric's machine during CS-F5 testing (and almost certainly explains this morning's "shortcuts vanished" mystery). Shortcuts recreated by hand; fix applied by the final fix agent (see install.ps1). Known pre-existing bug filed for the bug round: shared staging\ folder races when two flavours install concurrently ("Download did not produce a file") - Retail-only machines unaffected.

### Round 20 (2026-09-05 00:00 - 01:20, deployed + pushed ~01:30) - adversarial bug pass (v1.6.1)
- 6 finders (CLI, server, SPA, host, installer, security) -> 20 confirmed by independent reproducers -> all fixed per component -> regression suite passed. Highs fixed: listener reachable over the LAN via Host-header spoofing (now bound to 127.0.0.1); no CSRF protection (Test-SameOriginRequest: non-GET requires same-origin Origin/Referer; the host already sets Referer, the protocol handler and deploy.ps1 were fixed by hand afterwards); CLI argument injection via ids with spaces (ConvertTo-SafeProcessArg on every argv element); idle auto-shutdown killing in-flight jobs; /api/open 'url' letting a space smuggle msedge flags (structural URL parse + exact host allow-list); shared staging\ racing across flavours (now flavours\<id>\staging). Mediums: -Uninstall now auto-detects scratch roots + survives locked files; .gitignore covers tray-state.json + the WebView2 profile; job history eviction of a running job; DPI-only monitor moves not resyncing the pane; Wago search out-of-order responses; ad-filter CSS not honouring a live toggle-off. Lows: int settings 500->400, bool settings parse "false"/"0"/"no".
- 01:10 follow-up (v1.6.1 final, repo 5de20ac): the security-1 fix (bind 127.0.0.1 only) made every "localhost" client pay a refused-IPv6 fallback (Windows resolves localhost to ::1 first) - deploy.ps1's own 15 s health check failed and pings took 1-2 s. Listener now binds http://127.0.0.1 AND http://[::1]; measured: server answers in 0.8 s, 5 localhost pings = 49 ms, LAN still refused. Also patched curseforge-handler.vbs (Referer) and deploy.ps1 (Origin) for the CSRF guard, which the fix agent had missed. UX review round launched (5 reviewers -> confirm -> fix -> visual verify) -> v1.7.0.

### Round 21 (2026-09-05 01:35 - 02:20, deployed + pushed ~02:25) - design/UX pass (v1.7.0)
- 5 reviewers (first-time player, brief compliance, copy, visual/themes, flows) -> 17 confirmed by independent confirmers -> all fixed -> visual verification across 14 themes passed. Highs: "Update all" silently updated IGNORED addons and the pill never showed "Ignoring updates" when an update existed (functional bug found through UX); the freshness headline never switched to "Checking..." during a real job; Dark/Light --text-faint failed AA on bg-1/bg-3 (now 6.07/4.57 and 5.82/4.61); the install-complete message and README.txt still advertised the deleted API key and the old "Browse" name; the tray said updates were merely "ready" after installing them on multi-flavour machines. Mediums: a third update-count badge on the nav item (removed - one fact, one place), Save/Load wording missed in toasts/dialogs/job labels, pasting a CurseForge page link into Add addon gave no guidance (now: "That looks like a CurseForge page link - type its ID number instead." + a hint of where the ID lives), Lofi brand cat truncating the wordmark, the accent button overflowing with long flavour labels (wraps), installer console still saying adopt/.toc, tray menu wording.

### Rounds 22-24 (2026-09-05 02:30 - 06:40, deployed + pushed ~06:50) - tests for everything (v1.8.0)
- tests\run-all.ps1 (layers static -> unit -> integration -> host -> spa -> fixture-acceptance -> perf placeholder; -Quick/-NoNetwork/-NoTray/-Only/-Json; last-report.json/.md; exit code), ~400 assertions: Pester 3.4 unit tests dot-sourcing the two big scripts (new dot-source guard), real-process integration tests (state/settings/CSRF/loopback bind/open allow-list/jobs/freshness/tray/startup/fan-out + CLI status/detection/scan/install/rollback), host --selftest and --tray-selftest marker assertions, a same-origin headless-Edge SPA harness (42 checks incl. one pill per row, one headline, Update all count excluding ignored, drawer real name, progress bar, switcher rules, theme grid order, banned terms) + a 14-theme contrast audit (438 pairs), fixture acceptance as a traceability index over FLAVORS-SPEC section 8. deploy.ps1 now runs -Quick before touching the live folder (-SkipTests overrides loudly). Quick 92 s, full 190 s.
- Verification mutation check: forcing the CSRF guard open, gutting the status pill, and making the Classic resolver fall back to Retail each turned the suite red with a pointing message; the deploy gate aborted before copying with a failing test, proceeded when green, proceeded with the loud warning under -SkipTests. One harness gap recorded: duplicating the headline INSIDE the same container is not detected (text-presence check) - queued for the next round.
- The suite itself found and fixed: 9 real WCAG contrast failures (composited chip text over tint) across 5 older themes; a --selftest deep-link timing race; a permanently-red static check (.gitignore cache/ gap) and a Start-TestServer port-free check; a jobs 409 test that could not fail.
