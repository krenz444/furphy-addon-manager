# Pending fixes / verifications for the next round (collected 2026-09-02 while round 4 runs)

1. [DONE round 4] [server] Build-CliArgs passes multiple ids as separate arguments (`-Only 1 2`, `-Remove 1 2`). In `powershell -File` mode only the first binds; the rest become unbound positional args and the CLI exits 2 with no output. Verified live: `addon-sync.ps1 -Only 925037 288981 -Json` -> exit 2, empty stdout; `-Only 925037,288981 -Json` works. Fix: join ids with commas into ONE argument for -Only/-Remove/-Ignore/-Unignore/-Unpin/-Add (and E11 bulk remove). Add a server regression test: sync job with ids [a,b] must produce two result rows.
2. [DONE round 5] [ui] Dev-only mock data (app.js ?mock=1) references WeakAuras, which no longer exists in Midnight; replace with real addon names (BigWigs, Platynator, Auctionator) so nobody is misled.
3. [DONE round 5] [docs] README/SPEC: state the game context (Midnight 12.x, Season 2 on 12.1.0) and that WeakAuras/aura updating is out of scope permanently.
4. [DONE round 5 as E13] [ui] My Addons: surface the compatibility audit signals - show the newest file's game versions / the .toc Interface vs the client build, and a "Not built for 12.1" chip when neither lists the current major.minor (data already available: CF file gameVersions + toc Interface). Add a filter chip "Stale". (Spec this properly in ROADMAP as E13 before building.)
5. [ui] On a completely fresh browser state (no localStorage), the automatic startup check opens the job panel EXPANDED with the log visible; E2 requires automatic checks to show the collapsed slim bar. Likely the collapse preference defaults to expanded when no stored value exists; default the auto-check panel to collapsed regardless of stored preference.
